[project]
name = "everalgo-workspace"
version = "0"
description = "EverAlgo monorepo workspace — algorithm library for memory extraction & ranking."
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
everalgo-core         = { workspace = true }
everalgo-boundary     = { workspace = true }
everalgo-clustering   = { workspace = true }
everalgo-rank         = { workspace = true }
everalgo-parser       = { workspace = true }
everalgo-user-memory  = { workspace = true }
everalgo-agent-memory = { workspace = true }
everalgo-knowledge    = { workspace = true }

[dependency-groups]
dev = [
  "everalgo-core",
  "everalgo-boundary",
  "everalgo-clustering",
  "everalgo-rank",
  "everalgo-parser",
  "everalgo-user-memory",
  "everalgo-agent-memory",
  "everalgo-knowledge",
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "respx>=0.21",
  "ruff>=0.8",
  "mypy>=1.10",
  "pre-commit>=4.0",
  "git-cliff>=2.6",
]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.format]
docstring-code-format = true

[tool.ruff.lint]
select = [
  "F", "E", "W",
  "I", "D", "UP",
  "B", "SIM", "C4", "C90", "PIE",
  "RUF100", "RUF018",
]
ignore = [
  "D100",
  "D102",
  "D104",
  "D105",
  "D107",
  "E501",
]

[tool.ruff.lint.isort]
known-first-party = ["everalgo", "tests"]

[tool.ruff.lint.per-file-ignores]
# Test functions document themselves through descriptive names; D103 / D401
# would force boilerplate docstrings without informational value.
"**/tests/**/*.py" = ["D103", "D401"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.mccabe]
max-complexity = 15

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests", "packages"]
# --import-mode=importlib is required for src-layout packages sharing the
# `everalgo.*` PEP 420 namespace, and to prevent test sub-directories (e.g.
# `types/`) from shadowing stdlib modules under pytest's default `prepend`
# import mode. Reference: https://docs.pytest.org/en/stable/explanation/pythonpath.html
addopts = "-ra --import-mode=importlib"

[tool.mypy]
python_version = "3.12"
strict = true
# Required so mypy can resolve `everalgo.*` across PEP 420 namespace
# packages spread over multiple distributions. ``mypy_path`` lists every
# distribution's ``src`` so mypy treats them as the package roots and
# does not mistake ``everalgo.types`` (a sub-namespace) for the stdlib
# ``types`` module.
namespace_packages = true
explicit_package_bases = true
mypy_path = "packages/everalgo-core/src:packages/everalgo-boundary/src:packages/everalgo-clustering/src:packages/everalgo-rank/src:packages/everalgo-parser/src:packages/everalgo-user-memory/src:packages/everalgo-agent-memory/src:packages/everalgo-knowledge/src"

# Tests need not be strict-typed: pytest fixtures, parametrize tuples and
# `dict[str, Any]` builders rarely benefit from `-> None` annotations on
# every function. Keeping ``strict`` enabled for src/ but relaxed for tests/
# matches pydantic-ai / dspy / sklearn convention.
[[tool.mypy.overrides]]
module = ["tests.*"]
disallow_untyped_defs = false
disallow_untyped_calls = false
disallow_incomplete_defs = false

# Pyright (used by Pylance / VSCode / Cursor) configuration lives in
# the standalone `pyrightconfig.json` at the repo root — Pylance
# auto-discovers it. Keeping pyright config out of pyproject.toml avoids
# Pylance edge cases where `[tool.pyright]` is not picked up reliably.
