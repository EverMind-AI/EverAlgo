"""Package-level pytest fixtures.

Placeholder — this conftest reserves the slot for shared fixtures (e.g. fake LLM client, episode builders)
introduced by sub-project 3 (Testing Toolkit). It currently exposes no fixtures.
"""
