[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "evercore-clustering"
version = "0.1.0"
description = "EverCore clustering: cluster_by_geometry / cluster_by_llm functional operators with ClusterState value object."
requires-python = ">=3.12"
license = "MIT"
readme = "README.md"
authors = [{ name = "EverMind" }]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3.12",
]
dependencies = [
  "evercore-core>=0.1.0,<2.0.0",
  "numpy>=1.26",
]

[tool.hatch.build.targets.wheel]
packages = ["src/evercore"]
