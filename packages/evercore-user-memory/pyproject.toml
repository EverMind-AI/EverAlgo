[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "evercore-user-memory"
version = "0.1.0"
description = "EverCore user memory: Episode / Foresight / AtomicFact / Profile extractors (re-exports boundary as Chat/WorkspaceMemCellExtractor)."
requires-python = ">=3.12"
license = "MIT"
readme = "README.md"
authors = [{ name = "EverMind" }]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3.12",
]
dependencies = [
  "asgiref>=3.0",
  "evercore-boundary>=0.1.0,<2.0.0",
  "evercore-core>=0.1.0,<2.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/evercore"]
