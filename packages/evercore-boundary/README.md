# evercore-boundary

MemCell boundary extractors — `ChatMemCellExtractor` / `WorkspaceMemCellExtractor` / `AgentMemCellExtractor`, plus shared `_tokenize` / `_force_split` / boundary prompt helpers.

See the umbrella project: [EverCore monorepo](../../README.md) and the architecture document at [`docs/design.md`](../../docs/design.md).
